prototype(CodeQ.Site:InstagramGallery) < prototype(Neos.Fusion:Collection) {
	@context.count = 10
	collection = ${CodeQ.Site.getInstagramFeed('users/self/media/recent', '&count=' + count)}

	itemName = 'instagram'
	itemRenderer = CodeQ.Site:Instagram

	@cache {
		mode = 'cached'
		maximumLifetime = '3600'
	}
}

prototype(CodeQ.Site:Instagram) < prototype(Neos.Fusion:Template) {
	templatePath = 'resource://CodeQ.InstagramHelper/Private/Templates/Instagram.html'
	instagram = ${instagram}
}
